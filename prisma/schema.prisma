datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Chat {
  id      Int     @id @default(autoincrement())
  message String
  image   String?
  name    String?
}
